# Placeholders only. No automatic .env loading. Export deliberately in your shell.
BOT_MODE=offline
ORDER_SUBMISSION_ENABLED=false
ETORO_API_KEY=
ETORO_USER_KEY=
# Dedicated market data credentials; never inherited from eToro. No automatic .env load.
ALPACA_API_KEY=
ALPACA_API_SECRET=
# Local control credentials are generated per dashboard process in runtime/control-token.
