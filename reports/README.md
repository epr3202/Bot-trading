# Informes

bot backtest y demo-offline crean reports/runs/{run_id}.json y .html, sin sobrescribir
resultados distintos con el mismo ID. experiments.jsonl registra ejecuciones append-only.
Los informes sintéticos llevan SYNTHETIC — NO EVIDENCE OF PROFITABILITY. Los resultados
del simulador de ciclo de vida y el replay por minutos se identifican por separado.
